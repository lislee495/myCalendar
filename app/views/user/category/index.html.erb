<h1>Categories</h1>

<% if current_user.categories%>
<ul>
  <% @categories.each do |category| %>
  <li><%=link_to category.name, user_category_path(category)%></li>
  <% end %>

</ul>
<% else %>
No categories found!
<% end %>
