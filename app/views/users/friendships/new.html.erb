<!-- <%= form_tag(user_friendships_path) do %>
<%= label_tag "Add a Friend" %>
<%= text_field_tag


list: "categories_autocomplete" %>
  <datalist id="categories_autocomplete">
    <% Category.all.each do |category| %>
      <option value="<%= category.name %>">
    <% end %>
  </datalist> -->
